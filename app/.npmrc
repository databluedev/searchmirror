# react-apexcharts@1.9 declares a peer on apexcharts>=4 while this tree is
# pinned to apexcharts 3. npm 7+ treats that as fatal, so every install here
# -- including ones run by tooling such as the shadcn CLI -- needs this.
legacy-peer-deps=true
